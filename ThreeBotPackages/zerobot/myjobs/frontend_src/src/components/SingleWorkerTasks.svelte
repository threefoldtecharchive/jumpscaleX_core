<script>
	// import TasksRendering from './TasksRendering.svelte';
	// import workersData from '/home/rafy/svelte/my-svelte-project/src/data.js';

	// export let params;

	// var allTasks = [];
	// let workers = workersData.workers;

	// var isError = false;
	// if (params && params.taskId != null && params.taskId != undefined) {
	// 	allTasks = getSingleWorkerTasks(params.taskId);
	// }

	// // Getting tasks for each worker
	// function getSingleWorkerTasks(workerId) {
	// 	var workerTasks = [];
	// 	var workerIndex;
	// 	workerIndex = workers.findIndex(worker => worker.pid == workerId);
	// 	if (workerIndex == -1) {
	// 		isError = true;
	// 		return;
	// 	}
	// 	workers[workerIndex].tasks.forEach(task => {
	// 		workerTasks.push(task);
	// 	});
	// 	return workerTasks;
	// }
</script>

<!--[Header]-->
<!-- <h1>Single worker Tasks</h1>
<TasksRendering  allTasks = {allTasks} isError={isError}/>			 -->
